[package]
name = "bme68x-reference-oracle"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
bme68x = { path = "../..", default-features = false, features = ["float"] }

[build-dependencies]
cc = "1.2"

[lints.rust]
unsafe_code = "allow"
